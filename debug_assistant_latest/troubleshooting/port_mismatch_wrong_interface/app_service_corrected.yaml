apiVersion: v1
kind: Service
metadata:
  name: port-mismatch-app-service
spec:
  selector:
    app: port-mismatch-wrong-interface-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8765  # FIX: Changed from 8000 to match container port
  type: NodePort
